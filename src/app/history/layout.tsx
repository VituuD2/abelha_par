import type { Metadata } from "next";

export const metadata: Metadata = { title: "Histórico | Abelha Par" };
export default function HistoryLayout({ children }: { children: React.ReactNode }) { return children; }

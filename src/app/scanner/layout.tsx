import type { Metadata } from "next";

export const metadata: Metadata = { title: "Bipagem | Abelha Par" };
export default function ScannerLayout({ children }: { children: React.ReactNode }) { return children; }
